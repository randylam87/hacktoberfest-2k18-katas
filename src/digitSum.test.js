import { digitSum } from './digitSum';

/*
 * Example of the assertions you can use for your tests:
 *
 * - Testing equality between the function's output and a constant:
 * expect(digitSum()).toEqual(123);
 *
 * - Testing that the function returns null:
 * expect(digitSum()).toBeNull();
 *
 * - Testing that the function returns a falsy value (eg. false, 0, "")
 * expect(digitSum()).toBeFalsy();
 *
 * - Testing that the function returns a truthy value (eg. true, 1, "abc")
 * expect(digitSum()).toBeTruthy();
 *
 * - Testing that the function throws
 * expect(() => { digitSum(); }).toThrow();
 */

describe('digitSum', () => {
  it('digitSum does this thing...', () => {
    // TODO Your own assertion here
  });

  it('digitSum does that other thing...', () => {
    // TODO Your own assertion here
  });

  it('digitSum does a very cool thing...', () => {
    // TODO Your own assertion here
  });
});
